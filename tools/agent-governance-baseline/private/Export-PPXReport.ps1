# TODO: build step — assembles the joined records into the flat §5 schema (including calculated
# columns: StalenessBucket, CapabilitiesTruncated), then exports to CSV. Every run must append the
# documented known-limitations block (§8 of PPXAgentGovernanceBaseline.md) to the output, not just
# this spec. Not implemented yet.
