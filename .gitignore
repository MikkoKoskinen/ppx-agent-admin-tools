# Local-only internal documents; never pushed to GitHub
/Internal-Docs/

# User's local settings — copied from ppx.settings.example.psd1, holds tenant-specific values
/ppx.settings.psd1

# VS Code local debug harnesses
Debug-*.ps1

# Generated tenant-specific reports (CSV + limitations sidecar)
/reports/

#Gitleaks scan output (config .gitleaks.toml IS committed - shared rule tuning)
/gitleaks-report.json
